<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.fortyeight.spring" />
	
	
	<!-- 파일업로드 resolver 등록하기 -->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- 파일 용량의 크기를 정할 것 : 10mb -->
		<beans:property name="maxUploadSize" value="10485760"/>
	</beans:bean>
	
	<!-- interceptor 등록하기 -->
	<interceptors>
		<interceptor>
			<!-- 전체중에서 -->
			<mapping path="/**"/>
			<!-- 이건 로그인 없이도 접근이 가능해 -->
			<exclude-mapping path="/user/login.do"/>
			<exclude-mapping path="/user/agreeUser.do"/>			
			<exclude-mapping path="/user/insertUser*"/>
			<exclude-mapping path="/user/insertMailSending.do"/>			
			<exclude-mapping path="/user/check*"/>			
			<exclude-mapping path="/user/find*"/>	
			<exclude-mapping path="/market/selMarket.do"/>
			<exclude-mapping path="/market/buyMarket.do"/>
			<exclude-mapping path="/market/marketView.do"/>
			<exclude-mapping path="/board/boardList.do"/>
			<exclude-mapping path="/board/boardView.do"/>
			<exclude-mapping path="/board/boardView.do"/>
			<exclude-mapping path="/**/*.png"/>
			<exclude-mapping path="/**/*.css"/>
			<exclude-mapping path="/**/*.PNG"/>
			<exclude-mapping path="/**/*.mp4"/>
			<exclude-mapping path="/**/*.jpg"/>
			
			<!-- login interceptor를 거쳐갈 것 -->
			<beans:bean class="com.fortyeight.spring.common.interceptor.LoginUserInterceptor"></beans:bean>
			
		</interceptor>
	</interceptors>
	
	
	<!-- jackson converter 등록 -->
	<beans:bean id="jacksonConverter" class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
	<beans:bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
		<beans:property name="messageConverters">
			<beans:list>
				<beans:ref bean="jacksonConverter"/>
			</beans:list>
		</beans:property>
	</beans:bean> 
	
</beans:beans>
